trigger:
- main

pool:
  name: Default

variables:
- name: color
  value: blue

stages:
- stage: myStage1
  displayName: 'My Stage'
  jobs:
  - job: myJob1
    displayName: 'My Job'
    steps:
    - script: |
        echo "My favorite color is $(blue)"
    - script: |
        echo "##vso[task.setvariable variable=color]orange"
      name: changeColor
- stage: myStage2
  displayName: 'My Stage 2'
  variables:
    color: $[stageDependencies.myStage1.myJob1.outptus['changeColor.color']]
  jobs:
  - job: myJob
    displayName: 'My job 2'
    steps:
    - script: |
        echo "I used to love ${{ variables.color }}"
    - script: |
        echo "But now my favorite color is $(color)"