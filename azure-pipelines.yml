trigger:
- main

pool:
  name: Default

stages:
- stage: misPewitos
  jobs:
  - job: misPewitosJob
    steps:
    - template: templates/doingStuffTemplate.yaml
    - template: templates/doingStuffTemplate.yaml
      parameters:
        pewito: 'muy enojona'
        gordis: 'muy dormilona'