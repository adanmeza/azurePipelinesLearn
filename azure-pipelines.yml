trigger:
- main

pool:
  name: Default

stages:
- stage: Build
  jobs:
  - job: MyJob
    strategy:
      matrix:
        dev:
          service: dev
          environment: test
        qa:
          service: qa
          environment: test
        prod:
          service: prod
          enironment: prod
    steps:
    - script: |
        echo "This job has the following service: $(service)"
        echo "The environment is: $(environment)"