trigger:
- main

pool:
  name: Default

stages:
- stage: paramsStage
  jobs:
  - job: paramJobs
    steps:
    - template: templates/doingStuffTemplate.yaml
      parameters:
        message: 'Hellooooooo'
    - template: templates/doingStuffTemplate.yaml
      parameters:
        message: 'I swear is true!'
        extraMessage: true
