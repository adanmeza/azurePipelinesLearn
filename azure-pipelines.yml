trigger:
- main

pool:
  name: Default

stages:
- stage: templateStage
  jobs:
  - job: templateJob
    steps:
    - template: templates/doingStuffTemplate.yaml
      parameters:
        message: 'Hello World!'
    
    - template: templates/doingStuffTemplate.yaml
      parameters:
        message: 'Hello again world'