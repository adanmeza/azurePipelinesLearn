trigger:
- main

pool:
 name: Default

parameters:
- name: environment
  type: object
  default:
    - qa
    - prod
    - dev

stages:
- stage: DeployStage
  jobs:
  - ${{ each env in parameters.environment }}:
    - template: templates/doingStuffTemplate.yaml
      parameters:
       environment: ${{ env }}