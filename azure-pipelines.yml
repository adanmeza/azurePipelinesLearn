trigger:
- main

pool:
  name: Default

stages:
- stage: justARegularStage
  jobs:
  - template: templates/doingStuffTemplate.yaml
    parameters:
      jobName: firstTempalteJob
      jobMessage: 'first time using a template to show this!'
  - template: templates/doingStuffTemplate.yaml
    parameters:
      jobName: secondTemplateJob
      jobMessage: 'Second time using a template to show this!'
  - template: templates/doingStuffTemplate.yaml
    parameters:
      jobName: thirdTemplateJob
      jobMessage: 'Third time using a template to show this!'