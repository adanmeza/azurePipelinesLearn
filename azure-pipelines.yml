trigger:
- main

pool: Default

stages:
- stage: Build
  displayName: 'BuildStage'
  jobs:
  - job: BuildJob
    displayName: 'Build Job'
    steps:
    - script: |
        echo "Hello Build!"
      displayName: 'Waving Build'
    - script: |
        echo "Good bye Build"
      displayName: 'Bye Build'