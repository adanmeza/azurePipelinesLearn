parameters:
- name: message
  default: ''
- name: extraMessage
  default: false

steps:
- script: |
    echo "This is the message: ${{ parameters.message }}"
- ${{ if eq(parameters.extraMessage,true) }}:
  - script: echo "The extraMessage parameter is true!"