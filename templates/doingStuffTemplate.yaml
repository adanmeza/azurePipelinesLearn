parameters:
  - name: env
    type: string
  - name: pwd
    type: string

jobs:
- deployment: 
  environment: ${{ parameters.env }}
  strategy:
    runOnce:
      deploy:
        steps:
        - script: |
            echo "The password is ${{ parameters.pwd }}" 