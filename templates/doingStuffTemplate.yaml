parameters:
- name: pewito
  default: 'Schnautzer'
- name: gordis
  default: 'labrador'

steps:
- script: echo "Pewito es un: ${{ parameters.pewito }}"
- script: echo "La gordis es un: ${{ parameters.gordis }}"