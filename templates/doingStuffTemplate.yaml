parameters:
- name: environment
  type: object
  default:
    - labs
    - prod
    - homeLab

jobs:
- ${{ each par in parameters.environment }}:
  - job: jobs_${{ par }}
    steps:
    - script: |
        echo "This is the: ${{ par }} environment"